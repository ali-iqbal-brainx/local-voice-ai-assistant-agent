# Use a Python 3.13 virtualenv for this folder (not the system's 3.14) — piper-tts
# doesn't ship 3.14 wheels yet. Verified working: `/opt/homebrew/bin/python3.13 -m venv .venv`.
livekit-agents>=1.0
livekit-plugins-openai
livekit-plugins-silero
faster-whisper
piper-tts
python-dotenv
